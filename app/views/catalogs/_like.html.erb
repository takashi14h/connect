<% if user_signed_in? %>
  <% if catalogs.like_user?(current_user.id) %>
    <%= link_to catalog_likes_path(catalog_id: catalogs.id), class: 'col-md-6 favorite-color', method: :delete, remote: true do %>
      <i class="glyphicon glyphicon-heart" aria-hidden="true"></i>
      <span>
        <%= like_count %>
      </span>
    <% end %>
  <% else %>
    <%= link_to catalog_likes_path(catalog_id: catalogs.id), class: 'col-md-6', method: :post, remote: true do %>
      <i class="glyphicon glyphicon-heart-empty" aria-hidden="true"></i>
      <span>
        <%= like_count %>
      </span>
    <% end %>
  <% end %>
<% else %>
  <i class="glyphicon glyphicon-heart-empty" aria-hidden="true"></i>
    <span>
      <%= like_count %>
    </span>
<% end %>